<!DOCTYPE html>
<html>
  <head>
    <title>ReviewSite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>


  <body>
    <header>
      <div class="header-left">
        <%= link_to "プロコミ！", root_path, class: "button logo" %>
        <%= search_form_for @q, url: result_path do |f| %>
          <%= f.search_field :name_cont, placeholder: 'スクール名で検索' %>
        <% end %>
      </div>
      <div class="header-right">
        <%= link_to "スクール一覧", schools_path, class: "button school-list-button" %>
        <%= link_to "ログイン", new_user_session_path, class: "button login-button" %>
        <%= link_to "新規登録", new_user_registration_path, class: "button signup-button" %>
      </div>
    </header>
    <div class="content">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      <%= link_to "登録", new_school_path %>
      <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
      <% if user_signed_in? %>
        <%= link_to "プロフィール", user_path(current_user) %>
      <% end %>
    </div>

    <footer>
      <p>&copy; 2022 Prokomi.</p>
    </footer>
  </body>
</html>
